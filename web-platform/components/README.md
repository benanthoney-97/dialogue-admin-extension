# Web Platform Components

This directory hosts reusable UI pieces (cards, lists, modals, etc.) used by the `web-platform` pages.

Add shared markup, scripts, or styles here if you split functionality out of `login.html`, `signup.html`, or `connections.html`.
